//import product from '../../product';

/*export default  function preInitialize(code, participantCode = null){
    return async (dispatch) => {
        await dispatch(contest.actions.fetchContest(code, participantCode));
        await dispatch(contest.actions.addActivityHistory(contest.constants.ACTIVITY_TYPE_VISIT));
    };
}*/
